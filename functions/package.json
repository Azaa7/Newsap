{
  "name": "newsap-functions",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions",
    "lint": "echo \"No lint configured\""
  },
  "dependencies": {
    "expo-server-sdk": "^4.0.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.0.1"
  }
}
